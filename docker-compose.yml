# Docker Compose configuration for SUCA API
# This file defines all services needed to run the application

version: '3.8'

services:
  # PostgreSQL Database Service
  db:
    image: postgres:16-alpine
    container_name: suca-db
    restart: unless-stopped
    environment:
<<<<<<< Updated upstream
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASS:-postgres}
      POSTGRES_DB: ${DB_NAME:-suca}
=======
<<<<<<< Updated upstream
      POSTGRES_USER: ${DB_USER:-cynmeiciel}
      POSTGRES_PASSWORD: ${DB_PASS:-tvduyeplmr}
=======
      POSTGRES_USER: ${DB_USER:-suca}
      POSTGRES_PASSWORD: ${DB_PASS:-suca}
>>>>>>> Stashed changes
      POSTGRES_DB: ${DB_NAME:-jmdict}
>>>>>>> Stashed changes
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      # Persist database data
      - postgres_data:/var/lib/postgresql/data
    networks:
      - suca-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # SUCA API Application Service
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: suca-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      # Database connection
      DB_HOST: db
      DB_PORT: 5432
<<<<<<< Updated upstream
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      DB_NAME: ${DB_NAME:-suca}
=======
<<<<<<< Updated upstream
      DB_USER: ${DB_USER:-cynmeiciel}
      DB_PASS: ${DB_PASS:-tvduyeplmr}
=======
      DB_USER: ${DB_USER:-suca}
      DB_PASS: ${DB_PASS:-suca}
>>>>>>> Stashed changes
      DB_NAME: ${DB_NAME:-jmdict}
>>>>>>> Stashed changes
      
      # API settings
      ENV: ${ENV:-dev}
      DEBUG: ${DEBUG:-true}
      
      # JWT Authentication
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      
      # CORS settings
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:5173,http://127.0.0.1:5173}
      
      # SQLAlchemy
      SQLALCHEMY_ECHO: ${SQLALCHEMY_ECHO:-false}
    depends_on:
      db:
        condition: service_healthy
    networks:
      - suca-network
    volumes:
      # Mount code for development hot-reload
      - ./src:/app/src
      - ./tests:/app/tests
      - ./scripts:/app/scripts
      - ./alembic:/app/alembic
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Optional: PgAdmin for database management
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: suca-pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@suca.local
      PGADMIN_DEFAULT_PASSWORD: admin
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    ports:
      - "5050:80"
    networks:
      - suca-network
    depends_on:
      - db
    profiles:
      # Only start with: docker-compose --profile tools up
      - tools

networks:
  suca-network:
    driver: bridge

volumes:
  postgres_data:
    driver: local